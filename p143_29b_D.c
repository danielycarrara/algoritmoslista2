#include<stdio.h>
float main()
{
    float X;
    scanf("%f",&X);
    if(X>0)
    {
        printf("numero positivo");
    }
    if(X<0)
    {
        printf("numero negativo");
    }
}
